
upstream products {
    server 172.17.0.8:8082;
}

server {
    access_log /var/log/nginx/api_access.log main; # Each API may also log to a separate file

    listen 8082;
    server_name _;

    # URI routing
    #

	location /products {
    proxy_pass http://products/products/;
	}
}
